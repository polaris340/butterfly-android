<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <PreferenceCategory android:title="@string/pref_title_new_picture_notifications">
        <!-- A 'parent' preference, which enables/disables child preferences (below)
             when checked/unchecked. -->
        <CheckBoxPreference
            android:key="@string/key_pref_notification"
            android:title="@string/pref_title_new_picture_notifications"
            android:defaultValue="true" />

        <!-- Allows the user to choose a ringtone in the 'notification' category. -->
        <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
        <!-- NOTE: RingtonePreference's summary should be set to its value by the activity code. -->
        <CheckBoxPreference
            android:dependency="@string/key_pref_notification"
            android:key="@string/key_pref_notification_sound"
            android:title="@string/pref_title_sound"
            android:defaultValue="true" />

        <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
        <CheckBoxPreference
            android:dependency="@string/key_pref_notification"
            android:key="@string/key_pref_notification_vibrate"
            android:title="@string/pref_title_vibrate"
            android:defaultValue="true" />
    </PreferenceCategory>

</PreferenceScreen>
